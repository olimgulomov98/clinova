server {
    listen 80;
    server_name {{ app.domain }};

    location /.well-known/acme-challenge {
        root /var/www/letsencrypt;
        try_files $uri $uri/ =404;
    }

    location / {
        rewrite ^ https://{{ app.domain }}$request_uri? permanent;
    }
}
